{
  "name": "manga",
  "version": "0.0.1",
  "description": "Manga publishing platform for a better world",
  "main": "index.js",
  "scripts": {
    "build": "webpack --env=dev --progress --profile --colors",
    "build:watch": "webpack --env=dev --progress --profile --colors --watch",
    "build:prod": "webpack --env=prod --progress --profile --colors -p",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Mezriss",
  "license": "ISC",
  "dependencies": {
    "css-loader": "^0.28.8",
    "extract-text-webpack-plugin": "^3.0.2",
    "normalize.css": "^7.0.0",
    "postcss-loader": "^2.0.10",
    "precss": "^2.0.0",
    "style-loader": "^0.19.1",
    "typebase.css": "^0.5.0"
  },
  "devDependencies": {
    "webpack": "^3.10.0"
  }
}
